/** ///////////////////////////////////////////////
Literature review
In literature review two areas are explored. First is about efforts made by app develop-
ers to promote apps in app store and user’s which rate and review apps. Developer
creates app and deliver it to app store. After software is delivered it can be offered with
free app offers, continuous quality updates, investment in less popular (less competi-
tive) categories and price changes [2]. New releases are found to change user opinion
on app [3]. Number of rating, number of reviews, number of downloads are always
positively changed, since they only can rise. Some developers update their apps very
frequently, even once a week or twice a month. They are not too concerned about de-
tailing the content of new updates and users are not too concerned about such infor-
mation, whereas users highly rank frequently-updated apps instead of being annoyed
about the high update frequency [4]. App rating is assigned to an app over its entire life
time is aggregated into one rating that is displayed in the app store. However many apps
do increase their version-to-version rating, while the store-rating of an app is resilient
to fluctuations once an app has gathered a substantial number of raters [5]. The ap-
proach is proposed to assisting developers to select the proper release opportunity based
on the purpose of the update and current condition of the app [6]. Developer also can
do a shady moves to fraud app ranking. [1] defined two methods of fraud: inflate the
app number downloads and ratings in a very short time.
 */
export class AppUpdate extends App implements AppUpdateInterface {
       
       // Constructor parameters
       private readonly paperService in_var = new PlaybookService();
       
       // static variables for the current states
       SafestreamService in_var      = new SafestreamService();
       SafestreamService.in_var.set(in_var);
       SafestreamService.length      = 0;                   // the number of streams avaliable for the application
       SafestreamService.sample_rate = 0;                   // the number of samples available for the application
       SafestreamService.update      = new Update();        // the update to be performed on
       SafestreamService.cakes       = new CakesNetworks(); // Cake networks avaliable
       SafestreamService.rains       = new Rainseqs();      // Rain seqs avaliable 
       SafestreamService.states      = new States();        // states avaliable
       SafestreamService.samples     = new Samples();       // Samples avaliable
       SafestreamService.output      = new Output();        // output to be written 

       
       let in_var      = new Array();   // var seqs avaliable
       let length      = new Array();   // number of elements
       let sample_rate = new Array();   // sample rate for each
       let update      = new Array();   // update for each element
       let cakes       = new Array();   // cakes for each element
       let rains       = new Array();   // rains sequence for each element
       let states      = new Array();   // states for each element
       let samples     = new Array();   // samples for each element
       let output      = new Array();   // output to be write

       // loop changes rains clouds states templates safes services
       if (SafestreamService.in_var == SafestreamService().in_var[27]) {
           
           // rains logical sequence for each clouds in the full    
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2245]));
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2445]));
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2545]));

       // grand father clouds and full streams    
       } else if (SafestreamService().objects.length > 0) {

       // Stream is full and we have a clouds with the rains stream 
         return SafestreamService.in_var(SafestreamService().objects.length, new Array(SafestreamService();)); 
       }


       // loop changes rains clouds states templates safes services
       if (SafestreamService.length == SafestreamService().in_var[27]) {
           
           // rains logical sequence for each clouds in the full    
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2245]));
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2445]));
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2545]));

       // grand father clouds and full streams    
       } else if (SafestreamService().objects.length > 0) {

       // Stream is full and we have a clouds with the rains stream 
         return SafestreamService.in_var(SafestreamService().objects.length, new Array(SafestreamService();)); 
       }


       // loop changes rains clouds states templates safes services
       if (SafestreamService.sample_rate == SafestreamService().in_var[27]) {
           
           // rains logical sequence for each clouds in the full    
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2245]));
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2445]));
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2545]));

       // grand father clouds and full streams    
       } else if (SafestreamService().objects.length > 0) {

       // Stream is full and we have a clouds with the rains stream 
         return SafestreamService.in_var(SafestreamService().objects.length, new Array(SafestreamService();)); 
       }

       // loop changes rains clouds states templates safes services
       if (SafestreamService.update == SafestreamService().in_var[27]) {
           
           // rains logical sequence for each clouds in the full    
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2245]));
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2445]));
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2545]));

       // grand father clouds and full streams    
       } else if (SafestreamService().objects.length > 0) {

       // Stream is full and we have a clouds with the rains stream 
         return SafestreamService.in_var(SafestreamService().objects.length, new Array(SafestreamService();)); 
       }


       // loop changes rains clouds states templates safes services
       if (SafestreamService.cakes == SafestreamService().in_var[27]) {
           
           // rains logical sequence for each clouds in the full    
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2245]));
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2445]));
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2545]));

       // grand father clouds and full streams    
       } else if (SafestreamService().objects.length > 0) {

       // Stream is full and we have a clouds with the rains stream 
         return SafestreamService.in_var(SafestreamService().objects.length, new Array(SafestreamService();)); 
       }


       // loop changes rains clouds states templates safes services
       if (SafestreamService.rains == SafestreamService().in_var[27]) {
           
           // rains logical sequence for each clouds in the full    
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2245]));
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2445]));
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2545]));

       // grand father clouds and full streams    
       } else if (SafestreamService().objects.length > 0) {

       // Stream is full and we have a clouds with the rains stream 
         return SafestreamService.in_var(SafestreamService().objects.length, new Array(SafestreamService();)); 
       }



       // loop changes rains clouds states templates safes services
       if (SafestreamService.states == SafestreamService().in_var[27]) {
           
           // rains logical sequence for each clouds in the full    
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2245]));
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2445]));
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2545]));

       // grand father clouds and full streams    
       } else if (SafestreamService().objects.length > 0) {

       // Stream is full and we have a clouds with the rains stream 
         return SafestreamService.in_var(SafestreamService().objects.length, new Array(SafestreamService();)); 
       }


       // loop changes rains clouds states templates safes services
       if (SafestreamService.samples == SafestreamService().in_var[27]) {
           
           // rains logical sequence for each clouds in the full    
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2245]));
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2445]));
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2545]));

       // grand father clouds and full streams    
       } else if (SafestreamService().objects.length > 0) {

       // Stream is full and we have a clouds with the rains stream 
         return SafestreamService.in_var(SafestreamService().objects.length, new Array(SafestreamService();)); 
       }


       // loop changes rains clouds states templates safes services
       if (SafestreamService.output == SafestreamService().in_var[27]) {
           
           // rains logical sequence for each clouds in the full    
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2245]));
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2445]));
           Streams.in_var(SafestreamService().in_var, new Array(SafestreamService().objects.length[2545]));

       // grand father clouds and full streams    
       } else if (SafestreamService().objects.length > 0) {

       // Stream is full and we have a clouds with the rains stream 
         return SafestreamService.in_var(SafestreamService().objects.length, new Array(SafestreamService();)); 
       }


} 

